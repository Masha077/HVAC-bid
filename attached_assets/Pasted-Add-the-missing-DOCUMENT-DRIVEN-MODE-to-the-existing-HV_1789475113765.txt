Add the missing DOCUMENT-DRIVEN MODE to the existing HVAC BIS frontend.

Do NOT redesign the existing application or remove any existing functionality.

The application must have two clearly selectable input modes:

1. REQUIREMENT-DRIVEN
2. DOCUMENT-DRIVEN

Place a polished mode selector immediately above the current requirement input area.

Use rounded segmented controls/cards with the existing HVAC BIS visual system:

Primary:
#92140C
#1E1E24
#FFF8F0

Use rounded icons with the existing icon style.

Default mode:
REQUIREMENT-DRIVEN

When REQUIREMENT-DRIVEN is selected:
Keep the existing natural-language requirement textarea and Analyze button.

When DOCUMENT-DRIVEN is selected:
Replace the requirement textarea with a large PDF upload workspace.

Document-driven upload should support:
- drag and drop
- browse files
- multiple PDFs
- upload progress
- remove uploaded file
- file name
- file size
- PDF icon
- document status

Display supported document types such as:
- Tender
- Bid Document
- BOQ
- HVAC Specification
- Technical Specification
- Drawing
- Quotation

Add a clear button:

Analyze Documents

For now, do NOT implement the actual OCR/document-processing backend.

Create clean frontend service abstractions so the Document-Driven mode can later connect to the SNS Agent Workbench backend.

The future document workflow is:

PDF Upload
→ OCR / PDF Extraction
→ Document Classification
→ HVAC Requirement Extraction
→ BOQ / Specification Extraction
→ Cross-Document Validation
→ Equipment Analysis
→ Commercial Analysis
→ Bid Deliverable Planning
→ Audit / Traceability

Do not fabricate document analysis results.

For the document mode, if no backend is connected, show an appropriate state such as:

"Document analysis will begin when the SNS document-processing workflow is connected."

Keep the existing Requirement-Driven functionality completely intact.

Also make the active mode visually obvious.

Use:
- Requirement icon for Requirement-Driven
- PDF/document icon for Document-Driven
- rounded edges
- minimal texture
- Michroma for section headings
- existing HVAC BIS color palette
- minimal animation

The two modes should feel like two entry points into the same HVAC Bid Intelligence and Validation System, not two separate applications.